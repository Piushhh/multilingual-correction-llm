# ============================================================
# requirements.txt — Runtime dependencies
# Multilingual Domain-Aware Document Correction
# Python 3.10+  (tested on 3.13.5 on Windows)
# ============================================================

# ── Member 1: Custom LLM ─────────────────────────────────────
torch>=2.2.0
tokenizers>=0.19.0

# ── Member 2: DocumentAI ─────────────────────────────────────
scikit-learn>=1.4.0
scipy>=1.12.0
joblib>=1.3.0
pillow>=10.0.0
# Tesseract OCR wrapper (requires Tesseract binary installed separately)
# See docs/WINDOWS_TESSERACT.md for installation instructions
pytesseract>=0.3.10

# ── Member 3: Correction Engine ──────────────────────────────
# Gemma baseline adapter (optional — can run in mock mode without these)
# transformers>=4.40.0
# peft>=0.10.0
# datasets>=2.18.0

# ── Shared / Integration ─────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
pyyaml>=6.0.0
python-multipart>=0.0.9

# ── Evaluation ───────────────────────────────────────────────
Levenshtein>=0.25.0
numpy>=1.26.0
pandas>=2.1.0
