# ============================================================
# requirements.txt — Runtime dependencies
# Multilingual Domain-Aware Document Correction
# Python 3.10+  (tested on 3.13.5 on Windows, CI on Ubuntu 3.11)
# ============================================================

# ── Member 1: Custom LLM ─────────────────────────────────────
torch>=2.2.0
tokenizers>=0.19.0

# ── Member 2: DocumentAI ─────────────────────────────────────
# OCR-critical: exact-pinned for reproducible CER/WER benchmarks
# (changing these versions may alter OCR output and invalidate paper results)
scikit-learn==1.6.1            # Classifier trained and serialized with this version
scipy>=1.12.0
joblib>=1.3.0
pillow>=10.0.0
rapidfuzz==3.14.6              # Fuzzy terminology matching — API stable since 3.x
langdetect>=1.0.9
pymupdf==1.28.2                # PyMuPDF PDF rendering — pin to avoid fitz API churn
pytesseract==0.3.13            # Tesseract Python wrapper (requires binary separately)
# See docs/WINDOWS_TESSERACT.md / docs/ for platform install instructions
opencv-python==5.0.0           # Image preprocessing (deskew, CLAHE, adaptive binarize)

# ── Member 3: Correction Engine ──────────────────────────────
# Gemma baseline adapter (optional — can run in mock mode without these)
# transformers>=4.40.0
# peft>=0.10.0
# datasets>=2.18.0

# ── Shared / Integration ─────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
pyyaml>=6.0.0
python-multipart>=0.0.9
python-dotenv
sentencepiece
# easyocr>=1.7.0  # Optional extra for comparison in evaluation experiments

# ── Evaluation ───────────────────────────────────────────────
Levenshtein>=0.25.0
numpy>=1.26.0
pandas>=2.1.0
matplotlib>=3.8.0
