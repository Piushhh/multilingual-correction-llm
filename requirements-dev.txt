# ============================================================
# requirements-dev.txt — Development + testing dependencies
# Multilingual Domain-Aware Document Correction
# Installs on top of requirements.txt
# ============================================================
-r requirements.txt

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0               # required by FastAPI TestClient
anyio>=4.0.0

# ── Linting / Formatting ─────────────────────────────────────
ruff>=0.4.0
black>=24.0.0

# ── Notebook / Debug ─────────────────────────────────────────
ipython>=8.0.0
matplotlib>=3.8.0
